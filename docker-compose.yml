# Umbrella docker-compose — integration testing only.
# For day-to-day development, use: cd alfredv && docker compose up -d
#
# This file exists for multi-product integration tests that need
# the umbrella directory structure (submodule paths, smoke-tests).
# It delegates to AlfredV's compose for the actual service definitions.

include:
  - path: alfredv/docker-compose.yml
